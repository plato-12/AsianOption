% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{indiff_bellman_engine_cpp}
\alias{indiff_bellman_engine_cpp}
\title{Utility-indifference Bellman engine (single theta)}
\usage{
indiff_bellman_engine_cpp(
  S0,
  K,
  T_mat,
  N,
  mu_vec,
  sigma,
  r_cont,
  lambda_I,
  kappa_I,
  eta_vec,
  rho,
  lambda_bar_T,
  lambda_bar_P,
  kappa_J,
  k_A,
  k_B,
  psi_cost,
  gamma_ra,
  Gamma_Q,
  ell_1,
  Q_bar,
  eps_exec,
  phi_cap,
  n_opt,
  I0,
  Q0,
  J0,
  control_set,
  n_logS,
  n_I,
  n_Q,
  n_J,
  n_R,
  asian_type,
  option_type,
  theta,
  accum_rule,
  monitor_mode,
  fix_w,
  accum_sd,
  grid_drift,
  store_policy,
  n_threads,
  engine_mode,
  verbose
)
}
\arguments{
\item{S0, K, T_mat, N}{Contract and time-grid parameters.}

\item{mu_vec}{Length-N vector of drifts.}

\item{sigma, r_cont}{Volatility and continuously compounded rate.}

\item{lambda_I, kappa_I}{Exogenous impact loading and mean-reversion rate.}

\item{eta_vec}{Length-N vector of impact noise amplitudes.}

\item{rho}{Shock correlation.}

\item{lambda_bar_T, lambda_bar_P, kappa_J}{Dealer execution-impact parameters.}

\item{k_A, k_B, psi_cost}{Temporary execution cost parameters.}

\item{gamma_ra}{Absolute risk aversion.}

\item{Gamma_Q, ell_1}{Quadratic and linear terminal liquidation charges on
inventory. There is no terminal charge on the dealer impact state.}

\item{Q_bar}{Inventory bound.}

\item{eps_exec}{Execution-price floor of the admissible set.}

\item{phi_cap}{Payoff cap; values <= 0 mean no cap.}

\item{n_opt}{Option notional.}

\item{I0, Q0, J0}{Initial exogenous impact, inventory and dealer impact state.}

\item{control_set}{Admissible trading rates; must contain 0.}

\item{n_logS, n_I, n_Q, n_J, n_R}{Grid sizes. \code{n_logS <= 0} asks the engine
to size the log-price grid so that one shock spans exactly one cell.}

\item{asian_type}{0 = arithmetic, 1 = geometric.}

\item{option_type}{0 = call, 1 = put.}

\item{theta}{-1 (long), 0 (baseline) or +1 (short).}

\item{accum_rule}{0 = left-endpoint, 1 = trapezoidal accumulator quadrature.
Ignored under discrete monitoring.}

\item{monitor_mode}{0 = continuous monitoring, 1 = discrete fixing dates.}

\item{fix_w}{Length-N vector of accumulator weights; entry \code{m} is
applied at step \code{m} and is non-zero only when \code{t_{m+1}} is a
fixing date. Used only when \code{monitor_mode = 1}.}

\item{accum_sd}{Accumulator grid half-width in standard deviations of the
running average; capped by the reachable-set bound.}

\item{grid_drift}{1 = let the log-price grid track the deterministic drift.}

\item{store_policy}{Whether to return optimal-policy paths.}

\item{n_threads}{Worker threads for the cached path; 0 or less lets
RcppParallel choose, and 1 runs the backward sweep serially.}

\item{engine_mode}{0 = cached path, 1 = reference path.}

\item{verbose}{Whether to print progress.}
}
\value{
A list with the value at the initial state, the grids, clamp
  diagnostics and (optionally) forward policy paths.
}
\description{
Solves the CARA dealer problem of design.md Section 2 for one value of
\code{theta} and returns the value function at the initial state.
}
\keyword{internal}

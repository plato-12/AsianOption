% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geometric_asian_diffusion.R
\name{price_geometric_asian_diffusion}
\alias{price_geometric_asian_diffusion}
\title{Geometric Asian Option Price in Exogenous Diffusion Limit}
\usage{
price_geometric_asian_diffusion(
  S0,
  K,
  r,
  sigma,
  T,
  lambda_T,
  I0,
  kappa,
  eta,
  rho = 0,
  option_type = "call",
  n_quad = 1000
)
}
\arguments{
\item{S0}{Initial stock price (positive).}

\item{K}{Strike price (positive).}

\item{r}{Risk-free rate (positive, typically close to 1 in discrete models).}

\item{sigma}{Volatility parameter (positive).}

\item{T}{Time to maturity (positive).}

\item{lambda_T}{Transient impact coefficient (non-negative).}

\item{I0}{Initial transient impact state (can be any real number).}

\item{kappa}{Mean reversion rate for transient impact (positive).}

\item{eta}{Noise amplitude for transient impact process. Can be:
- A single positive number (constant eta)
- A function of time t in [0,T] returning a non-negative value}

\item{rho}{Correlation between stock and impact Brownian motions (in [-1,1]).}

\item{option_type}{Character string: "call" (default) or "put".}

\item{n_quad}{Number of quadrature points for numerical integration (default: 1000).}
}
\value{
The price of the geometric Asian option in the exogenous diffusion limit.
}
\description{
Computes the closed-form price for a geometric Asian call option in the
exogenous diffusion limit with transient price impact. This implements
the formula from Section 3.2.3 of the paper.
}
\details{
In the exogenous regime with no trading control (nu = 0), the stock price
dynamics are:

  dS_t = S_t * (r + lambda_T * I_t) dt + sigma * S_t * dW_t
  dI_t = -kappa * I_t dt + eta(t) * dW^I_t

where W and W^I are Brownian motions with correlation rho.

The running log-integral Z_T = integral_0^T log(S_u) du is Gaussian,
so G_T = exp(Z_T/T) is lognormal. This yields a Black-Scholes type
closed form:

  U(0) = exp(-r*T) * [exp(mu_G + sigma_G^2/2) * Phi(d1) - K * Phi(d2)]

where:
  - mu_G = m_Z / T
  - sigma_G^2 = v_Z / T^2
  - m_Z and v_Z are the mean and variance of Z_T
  - Phi is the standard normal CDF
  - d1 = (mu_G - log(K) + sigma_G^2) / sigma_G
  - d2 = d1 - sigma_G
}
\examples{
# Example 1: Constant eta, no correlation
price_geometric_asian_diffusion(
  S0 = 100, K = 100, r = 0.05, sigma = 0.2, T = 1,
  lambda_T = 0.01, I0 = 0, kappa = 1, eta = 0.1, rho = 0
)

# Example 2: Time-dependent eta
eta_func <- function(t) 0.1 * (1 + 0.5 * t)
price_geometric_asian_diffusion(
  S0 = 100, K = 100, r = 0.05, sigma = 0.2, T = 1,
  lambda_T = 0.01, I0 = 0.5, kappa = 2, eta = eta_func, rho = 0.3
)

}
\references{
Section 3.2.3 of "Asian option valuation under price impact"
}

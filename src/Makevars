## Rcpp flags (using modern Rcpp defaults)
## OpenMP is optional: the indifference engine falls back to a serial loop
## when SHLIB_OPENMP_CXXFLAGS is empty (see src/indiff_bellman.cpp).
PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS)
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS)
